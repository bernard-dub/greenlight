<% content_for :title do %>
Accueil
<% end %>


<div class="w-full container">

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl text-darkgreen">Nos réalisations et nos idées</h1>
    <%= render partial: 'new_card_button' %>
  </div>
  
  <div>
	  Explorer par :
  	<div>Localités : <%= render partial: 'tag', collection: ActsAsTaggableOn::Tag.for_context(:locations) %></div>
	<div>Thèmes : <%= render partial: 'tag', collection: ActsAsTaggableOn::Tag.for_context(:topics) %></div>
	<div>Statuts : <%= render partial: 'tag', collection: ActsAsTaggableOn::Tag.for_context(:statuses) %></div>
  </div>

  <div class="py-10 grid grid-cols-1 sm:grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 xxl:grid-cols-6 gap-5">
      <%= render @cards %>
  </div>
</div>

